# TODO

## Open questions for Nick

- 2026-09-03 — macOS camera path is **verified working**: `scripts/rs_probe.sh` streamed 30 fps on the first open after a re-plug and on a clean re-open (D435, USB 3, sudo). The failures earlier that day were a cascade of failed one-shot opens racing Apple's `UVCAssistant` (docs/realsense.md Troubleshooting). Remaining question: do you want the SAM backend proved on this Mac now that the live camera works, or wait for the Jetson?
- 2026-09-02 — D435 USB-C location: Intel's drawing doesn't dimension the connector face. The bracket leaves the camera's back and both ends free (5 mm above the plate), which covers both a rear and an end exit — please confirm on the unit which face it is (spec §6 A1) before the second print.
- 2026-09-02 — Bracket clocking: default `ARM_ANGLE_DEG=0` puts the camera 90° from the dowel / tool-I/O connector. Do you want it opposite the connector (180°) so the USB and tool cables route on different sides?
- 2026-09-02 — On-controller deployment was not attempted (robot at 192.168.1.50 unreachable today; URSim can't see USB). With the Jetson as the target, do you still want a feasibility poke at the CB's Debian when the robot is back on the network?
- 2026-09-02 — `Dockerfile.perception` pins librealsense at a tag verified to exist; the Jetson (JetPack / L4T) base image for CUDA + SAM is left as a follow-up — which JetPack version will the Orin run?
- 2026-09-02 — SAM backend (`--segment-backend sam`) is written against transformers 5.16's SamProcessor/SamModel API but was not run against weights in this session (no torch installed here). OK to leave as "wired, unverified", or should I pull torch + sam-vit-base and prove it on the Mac?
- 2026-09-04 — URSim 5.26 does **not** boot on this Mac Studio under Docker Desktop's Rosetta emulation (compose v2.23.3): Xvfb dies with `Trace/breakpoint trap` at entrypoint, so PolyScope (and with it the Dashboard on 29999) never comes up, and URControl stops listening on 30001 within minutes; the two `Exited (101)` containers from 7 weeks ago look like the same thing. Host port 5900 is also already taken (Screen Sharing?), which blocks `make sim-up` outright. The new `ur_flange_pose` URScript (`get_tcp_offset()` + `pose_trans/pose_inv`) is therefore verified only against the fake controller; `tests/test_integration_ursim.py::test_flange_pose_matches_the_controllers_own_arithmetic` will run in CI. Is switching Docker Desktop from Rosetta to QEMU emulation (Settings → General) acceptable on this box, or should real-controller checks wait for the UR10 at 192.168.1.50?
- 2026-09-04 (later) — Tried both emulation modes for URSim on the Mac Studio (Docker Desktop 4.26.1, macOS 26): **Rosetta** → Xvfb `Trace/breakpoint trap`, PolyScope/Dashboard never start (and even `ps` inside the container hits a Rosetta runtime assertion, `ThreadContextFcntl.cpp:178 is_rosetta_process`); **QEMU** → display comes up but URControl exits at boot (`Mutex::Mutex() … Error code = 95`, unsupported pthread mutex attribute). Docker settings restored to Rosetta. Options: upgrade Docker Desktop (4.26 is Dec 2023; newer builds changed the Rosetta/VMM plumbing), or run URSim on an amd64 box / let CI do it. Which do you want? Until then the send-to-robot flow is verified against the fake controller only; the real UR10 at 192.168.1.50 is the other route.
- 2026-09-04 (root cause) — Pinned it down: under Rosetta on this Mac the container's **Xvfb dies the moment any X client connects** (proved by hand: a fresh `Xvfb :3` lived until `x11vnc -display :3` attached, then vanished; `ps` and `tr` inside the container also die with a Rosetta runtime assertion `ThreadContextFcntl.cpp:178 is_rosetta_process`). PolyScope needs that display, so it never starts, and the Dashboard on 29999 (served by PolyScope) never listens; URControl runs but does not execute Primary scripts without PolyScope up. Same result via `docker compose` — not an invocation difference. Xcode CLT are also missing (`xcode-select -p` fails), which smells like a recent macOS major upgrade reset things; Rosetta itself may be the regression. Try in this order: `softwareupdate --install-rosetta`, then a Docker Desktop upgrade, then check whether the PolyScope X sim (web UI on :8000, no X server) has an arm64 image.
- 2026-09-04 (later) — PolyScope X sim: pinned `docker-compose.yml` back to **10.13.0** because 10.14.0-0.10.777's Services toggles never stick (firewall `ethCB0` 404 — comment in the compose file). Dependabot will try to re-bump it: OK to add an `ignore` rule for `universalrobots/ursim_polyscopex` in `.github/dependabot.yml`, or do you want to re-test each new tag by hand?
- 2026-09-04 (decision) — Nick: the perception / send-to-robot work is **PolyScope X exclusively** from here. Follow-up question: should `UR_PLATFORM` default to `polyscopex` (and the Robot-API port to 8000) in `RobotConfig`, so `perception gui` and `urctl` need no env exports against the PSX sim? It flips the default for every urctl user of this repo, so I left it as an opt-in for now.
- 2026-09-12 — Cell profiles ship with `UR_HOST` empty for `ur3` and `ur20` (`perception/cells/*.env`). What are the controller IPs, and is the "UR3" a UR3e running PolyScope X (the cell files assume PolyScope X: Robot-API :80, Primary :30001)? If it is PolyScope 5 / e-Series, set `UR_PLATFORM=e-series` in `ur3.env` — the doctor will flag the mismatch either way.
- 2026-09-12 — The Windows path (`scripts/setup-windows.ps1`, `scripts/cockpit.ps1`, `REALSENSE_LIB` at the SDK's default `bin\x64\realsense2.dll`) is written against the librealsense repo's own references but has NOT been run on a Windows box yet. First run on the work laptop is the verification; please paste the doctor output (`uv run perception --cell ur20 doctor --stream --json`) if anything fails.
- 2026-09-12 — Hand-eye: both bracket seeds are nominal (`BRACKET_SEEDS`, mm + ~1° error expected). Do you want a calibration routine in the cockpit before the demo (e.g. touch a known point from 3–4 poses and solve `T_flange_camera`), or is the nominal seed acceptable for the demo's standoff approach?
- 2026-09-12 — "Eventually on the NVIDIA GPU internal to the UR controller": is that the UR AI Accelerator (PolyScope X + Jetson AGX Orin)? If so, the deploy target is a URCap-X container; `Dockerfile.perception` is a plain linux/arm64 image today. Confirms which JetPack/L4T base to pin.
- 2026-09-12 — Push to `Olympus-Controls/UR-utils` is still 403 for JimothyJohn (pull-only). Work is committed locally on `feature/realsense-desktop-client`. Fork, or get push granted?
